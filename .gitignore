target/
# Host-local Cargo config (e.g. forcing x86_64 target on Apple Silicon with
# Intel-only Homebrew). Must NOT be committed — a fixed [build] target here
# would break native Linux / Raspberry Pi / CI builds.
.cargo/
.DS_Store
*.pdf
BACKUPS/
mnt/
.claude/
.cursor/
opentinyphotoapp.txt

# Local secrets / machine-specific config (never commit)
deploy.local.env
*.local.env
config.toml
!config.example.toml
.env
.env.*
!.env.example
credentials*.json
*credentials*.json
client_secret*.json
*client_secret*
*.pem
*.key
!*.example.key

# Generated analysis graphs (contain absolute local paths)
graphify-out/

# Local scratch leftovers — never commit
scratch.py
scratch_*.rs
temp_tests.txt
.agents/

